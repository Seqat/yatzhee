# Yahtzee — Python Dice Game
# No external dependencies required.
# All modules used (tkinter, random, collections, itertools) are part of the Python standard library.

# Development dependencies
pytest>=7.0
