# Runtime dependencies (none required - uses standard library only)
# All modules used (tkinter, random, collections, itertools) are part of the Python standard library.

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
black>=23.0
ruff>=0.1.0
